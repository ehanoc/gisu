import React, {Component} from 'react'

import classes from './index.scss'

const Background = () => (
  <div className={classes.Background}>
    <img className={classes.BackgroundImage} src="http://via.placeholder.com/800x600" />
  </div>
)

export default Background
